function response(
    room,
    msg,
    sender,
    isGroupChat,
    replier,
    imageDB,
    packageName
  ){
    // 코인 가격들 불러오기
    if(msg.indexOf("/코인") != -1){
        var upbitBTC = org.jsoup.jsoup.connect(
            ""
        )
    }  
  }